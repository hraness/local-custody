# Virtual workspace rooted at the repository so the crate is consumable as a
# cargo git dependency (`local-custody = { git = "...", rev = "..." }` resolves
# through the root manifest). Cargo build output lands in the repository-root
# `target/` directory regardless of the invocation directory.
[workspace]
members = ["rust"]
resolver = "2"
